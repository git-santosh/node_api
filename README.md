# API call to saved.io Application

Building a simple API for fetching data from `www.saved.io` application.
## Getting Started  

## Running the API program
install node modules by using ```npm i --save ```

browse ``` http://localhost:3000 ``` 